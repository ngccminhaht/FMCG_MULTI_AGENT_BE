name: fmcg-multi-agent

services:
  postgres:
    image: postgres:16.6-alpine
    container_name: fmcg-mvp-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-fmcg_mvp}
      POSTGRES_USER: ${POSTGRES_USER:-fmcg}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-fmcg}
    ports:
      - "5432:5432"
    volumes:
      - fmcg_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

volumes:
  fmcg_postgres_data:
